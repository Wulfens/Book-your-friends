<h1>Friends®</h1>
<h2>Have fun with an animal for a day ~</h2>

<div class="container-cards-animals">
  <% @animals.each do |animal| %>
    <div class="card-animal">
      <p><%= animal.name %></p>
      <p><%= animal.species %></p>
      <p><%= animal.price_per_hour %></p>
    </div>
  <% end %>
</div>
<%= link_to "Add A New Friend", new_animal_path %>
